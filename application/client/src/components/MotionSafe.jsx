/**
 * MotionSafe.jsx
 * --------------
 * A wrapper component that respects the user's prefers-reduced-motion setting.
 * Currently a placeholder for future accessibility improvements.
 *
 * Contributors: Ranjiv Jithendran, Team 02
 */
import React from 'react';
// MotionSafe component - unused for now but kept for future use
export function MotionSafe({ children, ...props }) {
  return <div {...props}>{children}</div>;
}

